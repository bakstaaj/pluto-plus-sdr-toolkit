# Repository Layout

```text
pluto-plus-sdr-toolkit/
  CMakeLists.txt
  README.md
  native/
    CMakeLists.txt
    src/
  configs/
  launchers/
  gui/
  docs/
  tools/
```

Native executables build into:

```text
build/native/
```

The project assumes a Pluto+ with RX coverage down to at least 70 MHz.
